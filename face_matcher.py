# from deepface import DeepFace

# Input: document_image (cv2 image matrix), selfie_image (cv2 image matrix)
# Output: confidence_score (Float 0-100)
# Work: Uses an AI model (like ArcFace) to find faces, extract vector embeddings, and calculate the cosine similarity to prove they are the same person.
def compare_faces(document_image, selfie_image):
    pass
